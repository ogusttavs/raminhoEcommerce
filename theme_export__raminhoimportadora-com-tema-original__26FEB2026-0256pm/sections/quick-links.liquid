<section class="section {% unless section.settings.show_desktop %}hidden-lap-and-up{% endunless %}" data-section-id="{{ section.id }}" data-section-type="quick-links">
  <div class="container">
    {%- if section.settings.title != blank -%}
      <header class="section__header">
        <h2 class="section__title heading h3">{{ section.settings.title | escape }}</h2>
        
        {%- if section.settings.link_title != blank and section.settings.link != blank -%}
          <a href="{{ section.settings.link }}" class="section__action-link link">{{ section.settings.link_title | escape }} {% render 'icon', icon: 'tail-right' %}</a>
        {%- endif -%}
      </header>
    {%- endif -%}

    <div class="quick-links">
      {%- for block in section.blocks -%}
        <a href="{{ block.settings.link }}" class="quick-links__link {% if section.settings.expand_items %}quick-links__link--grow{% endif %} heading" {{ block.shopify_attributes }}>
          {{- block.settings.title | escape -}}
        </a>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Links rápidos",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Título",
      "default": "Links rápidos"
    },
    {
      "type": "checkbox",
      "id": "expand_items",
      "label": "Expandir itens para preencher a linha",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_desktop",
      "label": "Mostrar em desktop",
      "info": "A seção será visível no editor de temas mesmo se esta opção estiver desmarcada para facilitar a edição.",
      "default": true
    },
    {
      "type": "text",
      "id": "link_title",
      "label": "Título do link"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Link"
    }
  ],
  "blocks": [
    {
      "type": "link",
      "name": "Link",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Título",
          "default": "Seu link"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "category": "Link",
      "name": "Links rápidos",
      "settings": {},
      "blocks": [
        {
          "type": "link"
        },
        {
          "type": "link"
        },
        {
          "type": "link"
        },
        {
          "type": "link"
        },
        {
          "type": "link"
        },
        {
          "type": "link"
        }
      ]
    }
  ]
}
{% endschema %}
