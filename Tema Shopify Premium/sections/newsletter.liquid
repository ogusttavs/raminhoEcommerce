<style>
  .newsletter-section {
    background-color: var(--color-background-alt); /* Ex: Branco puro para destaque limpo */
    padding: 5rem 0;
    text-align: center;
    border-top: 1px solid rgba(0,0,0,0.05);
  }

  .newsletter-inner {
    max-width: 600px;
    margin: 0 auto;
  }

  .newsletter-title {
    font-size: 2rem;
    color: var(--color-primary-dark);
    margin-bottom: 0.5rem;
  }

  .newsletter-subtitle {
    color: var(--color-secondary);
    font-size: 1.1rem;
    margin-bottom: 2rem;
  }

  .newsletter-form-group {
    display: flex;
    gap: 0.5rem;
    max-width: 450px;
    margin: 0 auto;
  }

  .newsletter-input {
    flex-grow: 1;
    padding: 1rem 1.5rem;
    border: 1px solid rgba(0,0,0,0.1);
    border-radius: var(--border-radius-pill);
    font-family: var(--font-body);
    font-size: 1rem;
    background-color: var(--color-background);
  }

  .newsletter-input:focus {
    outline: none;
    border-color: var(--color-primary);
  }

  .newsletter-btn {
    padding: 1rem 2rem;
    border-radius: var(--border-radius-pill);
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(-10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media(max-width: 500px) {
    .newsletter-form-group {
      flex-direction: column;
    }
  }
</style>

<section class="newsletter-section">
  <div class="container newsletter-inner">
    <h2 class="newsletter-title font-heading">Acesso Exclusivo à Colheita.</h2>
    <p class="newsletter-subtitle">Assine para receber convites para colheitas exclusivas e safras limitadas antes de todo mundo.</p>
    
    {% form 'customer', class: 'newsletter-form' %}
      <input type="hidden" name="contact[tags]" value="newsletter">
      
      {% if form.posted_successfully? %}
        <div class="newsletter-success" style="background: rgba(13, 124, 74, 0.1); padding: 1.5rem; border-radius: 12px; color: var(--color-primary-dark); font-weight: 600; margin-bottom: 2rem; animation: fadeIn 0.5s ease;">
           🎉 Colheita agendada! Você agora faz parte do círculo exclusivo Raminho.
        </div>
      {% elsif form.errors %}
        <div class="newsletter-error" style="color: #c53030; margin-bottom: 1rem; font-size: 0.9rem;">
          {{ form.errors | default_errors }}
        </div>
      {% endif %}

      <div class="newsletter-form-group">
        <input type="email"
               name="contact[email]"
               class="newsletter-input"
               value="{% if customer %}{{ customer.email }}{% endif %}"
               placeholder="Seu endereço de e-mail"
               autoCorrect="off"
               autoCapitalize="off"
               required>
        <button type="submit" class="btn btn--primary newsletter-btn" name="commit">
          Assinar
        </button>
      </div>
    {% endform %}
  </div>
</section>

{% schema %}
{
  "name": "Newsletter Premium",
  "presets": [
    {
      "name": "Newsletter Clássica"
    }
  ]
}
{% endschema %}
