{{ 'product-card.css' | asset_url | stylesheet_tag }}

<style>
  /* Slider Horizontal Apple Centered Base */
  .netflix-slider-section {
    padding: 4rem 0 5rem 0;
    overflow: hidden;
  }

  .netflix-slider-header {
    margin-bottom: 2rem;
    text-align: left;
  }

  .netflix-slider-title {
    font-size: 2.2rem;
    color: var(--color-primary-dark);
  }

  .netflix-slider-description {
    color: var(--color-secondary);
    font-size: 1.1rem;
    margin-top: 0.5rem;
  }

  .netflix-track-container {
    width: 100%;
    max-width: 1440px;
    margin: 0 auto;
    position: relative;
    padding: 0 1.5rem;
  }

  .netflix-track {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    gap: 1.5rem;
    padding: 0 0 2.5rem 0;
    -ms-overflow-style: none;
    scrollbar-width: none;
    justify-content: center; /* Mantém centralizado por padrão */
  }

  /* Se os produtos estourarem a largura, o justify-content: center pode quebrar o início,
     então usamos flex-start apenas quando há overflow */
  @media (max-width: 1440px) {
    .netflix-track {
      justify-content: flex-start;
    }
  }

  .netflix-track::-webkit-scrollbar {
    display: none;
  }

  .netflix-slide {
    flex: 0 0 280px; 
    scroll-snap-align: start;
    transition: transform 0.3s ease;
  }

  @media (max-width: 768px) {
    .netflix-track-container {
      padding: 0 1rem;
    }
    .netflix-track {
      gap: 1rem;
      justify-content: flex-start;
    }
    .netflix-slide {
      flex: 0 0 240px;
      scroll-snap-align: center;
    }
  }


  /* Container Relativo para setas Absolutas */
  .netflix-carousel-wrapper {
    position: relative;
    width: 100%;
  }

  /* Setas Navegação */
  .netflix-nav-btn {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: #fff;
    border: 1px solid rgba(0,0,0,0.1);
    box-shadow: 0 4px 10px rgba(0,0,0,0.08);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s;
    color: var(--color-primary-dark);
    position: absolute;
    top: 40%;
    transform: translateY(-50%);
    z-index: 10;
  }
  .netflix-nav-btn:hover {
    background: var(--color-primary);
    color: #fff;
    transform: translateY(-50%) scale(1.05);
  }
  
  .netflix-nav-btn.btn-prev { left: -22px; }
  .netflix-nav-btn.btn-next { right: -22px; }

  @media (max-width: 768px) {
    /* Mantém as setas visíveis por acessibilidade (Público Sênior) mas traz elas para dentro do carrossel */
    .netflix-nav-btn {
      width: 38px;
      height: 38px;
    }
    .netflix-nav-btn.btn-prev { left: 4px; }
    .netflix-nav-btn.btn-next { right: 4px; }
  }
</style>

<section class="netflix-slider-section" style="background-color: var(--color-background);">
  <div class="container">
    <div class="netflix-slider-header">
      <h2 class="netflix-slider-title font-heading">{{ section.settings.title }}</h2>
      {% if section.settings.description != blank %}
        <p class="netflix-slider-description">{{ section.settings.description }}</p>
      {% endif %}
    </div>
  </div>

  <!-- Pista de Deslizamento Fora do Container para Bleed infinito -->
  <div class="netflix-carousel-wrapper">
    <div class="container" style="position:relative; height: 0; z-index: 10;">
      <!-- Arrows alinhadas com o conteúdo -->
      <button class="netflix-nav-btn btn-prev" aria-label="Anterior" id="prev-{{ section.id }}" style="left: -22px;">
        <svg width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" /></svg>
      </button>
      <button class="netflix-nav-btn btn-next" aria-label="Próximo" id="next-{{ section.id }}" style="right: -22px;">
        <svg width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" /></svg>
      </button>
    </div>

    <div class="netflix-track-container">
      <div class="netflix-track" id="track-{{ section.id }}">
      {% assign collection = collections[section.settings.collection] %}
      
      {% if collection != blank and collection.products_count > 0 %}
        {% for product in collection.products limit: section.settings.limit %}
          <div class="netflix-slide">
            {% render 'product-card-premium', product: product %}
          </div>
        {% endfor %}
      {% else %}
        {% for i in (1..6) %}
          <div class="netflix-slide">
            <div class="product-card-premium" style="opacity: 0.8;">
              <div class="product-card-image-wrapper" style="background: #f0f0f0; display:flex; align-items:center; justify-content:center; height: 350px;">
                  <svg width="60" height="60" viewBox="0 0 24 24" fill="none" stroke="#ccc" stroke-width="1"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
              </div>
              <div class="product-card-info" style="padding: 1.5rem;">
                 <h3 class="font-heading" style="font-size: 1.2rem; margin-bottom: 0.5rem;">Fruta Premium Exemplo {{ i }}</h3>
                 <p style="color: var(--color-primary); font-weight: 700;">R$ 29,99</p>
              </div>
            </div>
          </div>
        {% endfor %}
      {% endif %}
      </div>
    </div>
  </div>
</section>

<!-- Script Navegação -->
<script>
  (function() {
    const track = document.getElementById('track-{{ section.id }}');
    const btnPrev = document.getElementById('prev-{{ section.id }}');
    const btnNext = document.getElementById('next-{{ section.id }}');

    if(track && btnPrev && btnNext) {
      btnNext.addEventListener('click', () => {
        const slide = track.querySelector('.netflix-slide');
        const scrollAmount = slide ? slide.offsetWidth + 24 : 344;
        track.scrollBy({ left: scrollAmount, behavior: 'smooth' });
      });

      btnPrev.addEventListener('click', () => {
        const slide = track.querySelector('.netflix-slide');
        const scrollAmount = slide ? slide.offsetWidth + 24 : 344;
        track.scrollBy({ left: -scrollAmount, behavior: 'smooth' });
      });
    }
  })();
</script>

{% schema %}
{
  "name": "Carrossel Horizontal",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Título da Seção",
      "default": "Frutas da Estação"
    },
    {
      "type": "text",
      "id": "description",
      "label": "Descrição Curta",
      "default": "O frescor absoluto colhido hoje."
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Coleção para Exibir"
    },
    {
      "type": "range",
      "id": "limit",
      "min": 4,
      "max": 12,
      "step": 1,
      "default": 8,
      "label": "Máximo de Produtos"
    }
  ],
  "presets": [
    {
      "name": "Carrossel Tipo Netflix"
    }
  ]
}
{% endschema %}
